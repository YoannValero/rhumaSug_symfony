<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a id="logo" class="navbar-brand" href="{{ path('accueil') }}"><img src="{{ asset('assets/images/bx_bx-circle.svg') }}" class="img-responsive img-circle" alt="logo" width="50px;"></a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a href="{{ path('accueil') }}">ACCUEIL</a>
                </li>
                <li>
                    <a href="{{ path('produits') }}">NOS PRODUITS</a>
                </li>
                <li>
                    <a href="{{ path('contact') }}">CONTACT</a>
                </li>
               
                {% if app.user %}
                   
                    <li>
                        <a href="{{ path('show_panier') }}"><img class="img-fluid" alt="Responsive image" src="{{ asset('assets/images/shopping-cart.png') }}" width="20px" height="20px" ></a>
                    </li>
                    <li>
                        <a href="{{ path('app_logout') }}"><img class="img-fluid" alt="Responsive image" src="{{ asset('assets/images/deconnexion-png-5.png') }}" width="20px" height="20px" ></a>
                    </li>
                
                    
                   
                    
                {% else %}

                    <li>
                        <a href="{{ path('app_login') }}">Connexion</a>
                    </li>  
                    <li>
                        <a href="{{ path('app_register') }}">Inscription</a>
                    </li>  
                {% endif %}
            </ul>
        </div>
    </div>
</nav>